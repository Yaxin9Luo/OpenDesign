# ---- Secrets ----
.env
.env.local
.env.*.local

# ---- Python ----
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# ---- Runtime artifacts ----
out/
sessions/

# ---- OS ----
.DS_Store
Thumbs.db

# ---- IDE ----
.vscode/
.idea/
*.swp
*.swo
*~

# ---- Local dev reference (not redistributed) ----
# WeChat blog seed reference — copyright, kept local only
design_agent_blog.pdf
# Cloned third-party reference repo, safety net (already moved outside project)
Paper2Any/

# ---- Claude Code local state ----
.claude/
dataset/

# ---- Private workspace / scratch ----
docs/.obsidian/
docs/MEETING-*.md
# iCloud / Finder duplicates (macOS creates `foo 2.py` / `bar 3.png` when a
# file is synced mid-read OR Cmd-D'd in Finder — they diverge from canonical
# and survive renames as orphans, e.g. `longcatdesign-brief 2.md` lingered
# after the v2.4.1 LongcatDesign→OpenDesign rename. Catch all extensions.
* [0-9].*
# One-off scratch file left at repo root; harmless but not product surface.
/design
